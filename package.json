{
    "name": "prisma-express-client-template",
    "version": "0.1.0",
    "description": "template for project",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "concurrently \"npm run dev --prefix server\" \"npm run start --prefix client-react\""
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "concurrently": "^9.2.1"
    },
    "devDependencies": {
        "prettier": "3.6.2"
    }
}
